#!/bin/sh
# Pre-commit hook: run tests (includes ArchUnit + coverage)
echo "🔍 Running tests..."
./mvnw verify -q
if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Commit aborted."
  exit 1
fi
echo "✅ Tests passed."
