name: CI Feature

on:
  push:
    branches: ['feature/**', 'feat/**']

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci

      # Commitlint
      - name: Commitlint
        run: npx commitlint --from $(git log --oneline origin/main..HEAD --format="%H" | tail -1)^ --to HEAD

      # TypeScript check
      - name: TypeScript check
        run: npx tsc --noEmit

      # ESLint
      - run: npm run lint

      # Build
      - run: npm run build

  open-pr:
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Create PR to develop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          EXISTING=$(gh pr list --head "${{ github.ref_name }}" --base develop --json number -q '.[0].number')
          if [ -z "$EXISTING" ]; then
            gh pr create --base develop --head "${{ github.ref_name }}" \
              --title "Merge ${{ github.ref_name }} into develop" \
              --body "Auto-created by CI after successful build."
          fi
